import styled from "@emotion/styled";
import { Allpost } from "../components/Main";

export const Main = () => {
  return (
    <MainContainer>
      <Allpost />
    </MainContainer>
  );
};

const MainContainer = styled.div``;
